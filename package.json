{
  "name": "agora",
  "version": "0.1.0",
  "description": "Company Brain with Secret Handshake — double-blind trade secret matching via PSI",
  "type": "module",
  "scripts": {
    "dev": "node server/index.js",
    "db:push": "npx prisma db push",
    "db:seed": "node scripts/seed.js",
    "setup": "npm install && npx prisma db push && node scripts/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "fastify": "^5.3.3",
    "@fastify/cors": "^11.0.0",
    "@fastify/static": "^8.1.0",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "prisma": "^6.9.0"
  }
}
